# Tasks

## 1. 规格与边界

- [x] 明确审批办理、发起审批、自定义按钮、关系配置和审批后关系处理的配置边界。
- [x] 记录 SDD 规格、任务、测试计划和执行日志。

## 2. 前端改造

- [x] 将原“动作配置”入口调整为业务处理语义，不再向普通对象设计暴露独立动作 DSL 菜单。
- [x] 普通对象设计不再展示“自动化动作”独立一级入口，审批后处理整合进“关系与级联”。
- [x] 重构 `BusinessActionDesigner.vue` 为业务向导式配置，不以 DSL 步骤树作为主界面。
- [x] 动态解析已有动作配置，识别流程回调、明细遍历、数量台账等业务含义。
- [x] 字段映射候选从主表/子表字段动态生成。
- [x] “关系与级联”直接维护明细关系、子表选择器、字段映射和审批后字段用途，不再让用户重复配置子表路径和循环变量。
- [x] 普通字段下拉隐藏 `item.xxx` 等内部表达式，未识别时显示中文维护提示。
- [x] 保留高级 JSON 折叠区供开发者兜底。
- [x] 关系配置页面按原型方向改为左侧步骤轨道 + 右侧业务卡片，减少表单堆叠。
- [x] 查询条件输入框不展示字段长度字数统计，避免搜索区出现 `0/200`。
- [x] 列表设计器增加显眼的“自定义操作”入口，可直接新增顶部按钮、行操作或配置全部操作。
- [x] 关系类型收敛为“包含多个明细”，隐藏其它关系类型选择。
- [x] 自定义操作删除最后一项时保留配置弹窗空状态，不让用户误以为下方配置区消失。
- [x] 业务流程配置去掉编号生成配置入口，编号规则回归表单设计字段配置。
- [x] 单据基础配置固定发起人为创建人 `createBy`，隐藏发起人选择和负责人字段。
- [x] 业务记录绑定去掉负责人字段，只保留流程状态、标题和必要表绑定信息。
- [x] 业务流程配置阶段与文案优化为“单据状态 / 流程绑定”，减少技术配置感。

## 3. 验证

- [x] 执行相关 Vue 文件 ESLint。
- [x] 执行前端构建或说明跳过原因。
- [x] 回填执行日志。
