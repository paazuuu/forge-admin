# 公式能力扩展 — 增量验证计划

## 本轮迁移验证范围

- P0 后端编译：`forge-plugin-generator` 及依赖模块编译通过。
- P0 公式单测：公式领域、校验、运行时相关测试通过。
- P0 Flyway 脚本：`formula_config` 字段迁移脚本具备防重复保护，且版本号不与现有脚本冲突。
- P0 主从聚合回写：从表新增/更新/删除后的父表 STORED 聚合刷新链路通过编译验证，真实库接口验证单独执行。
- P1 前端构建：字段属性面板公式配置相关 Vue/JS 能通过生产构建。

## 暂不覆盖

- 真实数据库下从表变更后的 STORED 聚合公式回写链路接口验证；本轮优先保证迁移代码编译、前端构建、配置保存/回显和动态 CRUD 代码链路闭环。

## 2026-06-13 编辑表单公式回显修复增量验证

- P0 前端静态检查：针对 `AiCrudPage.vue`、动态 CRUD 页面、低代码运行态预览、字段属性面板执行 ESLint。
- P0 差异检查：执行 `git diff --check`，确认无空白错误。
- P0 前端构建：执行 `pnpm --dir forge-admin-ui build`，确认公式运行态计算和动态页兼容改动可生产构建。
- 暂不覆盖：未启动真实后端和数据库进行动态页接口级回显验证；本轮修复范围集中在前端依赖同步、运行态计算触发和旧运行态配置兼容。
